{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/simulator/bin/simulator",
            "args": [
                "--config", "${workspaceFolder}/simulator/config.yaml",
                "--config", "${workspaceFolder}/simulator/config-local.yaml",
            ]
        },
    ]
}